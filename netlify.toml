[build]
  command = "if [ -d \"flutter\" ]; then cd flutter && git pull && cd ..; else git clone -b stable https://github.com/flutter/flutter.git; fi; flutter/bin/flutter clean; rm -rf ~/.pub-cache; flutter/bin/flutter pub get; flutter/bin/flutter build web"
  publish = "build/web"

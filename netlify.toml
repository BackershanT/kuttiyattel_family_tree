[build]
  command = "if [ -d \"flutter\" ]; then cd flutter && git pull && cd ..; else git clone -b stable https://github.com/flutter/flutter.git; fi; flutter/bin/flutter clean; flutter/bin/flutter pub get; flutter/bin/flutter build web"
  publish = "build/web"

[build]
  # Consolidated build command: clones flutter, sets path, installs deps, and builds web
  command = """
    if [ ! -d "$HOME/flutter" ]; then 
      git clone https://github.com/flutter/flutter.git -b stable "$HOME/flutter"; 
    fi && \
    export PATH="$HOME/flutter/bin:$PATH" && \
    # Visible folder ensures stable ../pub-cache paths in package_config.json
    export PUB_CACHE="$PWD/pub-cache" && \
    flutter doctor && \
    flutter clean && \
    rm -rf .dart_tool && \
    flutter pub get && \
    # DEBUG: Verify package is in the custom cache
    ls "$PUB_CACHE/hosted/pub.dev" | grep graphview && \
    flutter build web --release \
      --dart-define=SUPABASE_URL=$SUPABASE_URL \
      --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
  """
  publish = "build/web"

[build.environment]
  # Ensuring the build environment uses the correct settings
  FLUTTER_VERSION = "stable"

[[redirects]]
  # Essential for Flutter Web routing (SPAs)
  from = "/*"
  to = "/index.html"
  status = 200
